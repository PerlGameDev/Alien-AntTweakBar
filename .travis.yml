language: perl
before_install:
 - sudo apt-get update
 - sudo apt-get install libdbus
 - sudo apt-get install libsdl1.2-dev

install: cpanm -v --installdeps --notest .

perl:
  - "5.18"
  - "5.16"
  - "5.14"
